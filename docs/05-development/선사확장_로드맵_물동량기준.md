# 화물추적 선사 로드맵 — 무료 선사 기준

작성 2026-08-11 · 전면 개정 2026-08-14 (요금 실사 — 유료 선사 전면 제외) · **현행화 2026-08-19**(ZIM 개통·MSC 제외 반영)
근거 데이터: `ELVIS_TWSC.FMS_MBL_MST` 컨테이너 MBL **119,631건** (2025-01-01 ~ 2026-08-11, FCL+LCL)

> ## ⚠ 커버리지 수치의 정본은 이 문서가 아니다
>
> **`docs/05-development/선사API_확장플랜_2026-08-19.docx` 의 2026-08-19 재실측이 최신·정본**이다.
> 재실측은 모집단이 다르고(ETD 2024-01-01 이후 **230,801건**), **`LINE_CD` 매핑 오류가 교정**됐다
> — SITC=`LHY` · 장금상선=`LSIK`+`LNMS` · HMM=`LHDMU`+`LAMR` 병합. 같은 선사가 대리점·서비스별로
> 복수 코드를 갖는데 이를 합산하지 않으면 순위와 비중이 그대로 왜곡된다(§7 참조).
>
> 그래서 **두 문서의 %를 섞어 쓰면 안 된다.** 본 문서의 수치는 2025-01-01 기준 119,631건 모집단에서만
> 유효하며, 대외 보고·의사결정용 커버리지는 재실측 docx 를 인용할 것. 본 문서는 **선사별 요금·약관
> 실사 결과와 신청 절차**의 근거로 계속 유효하다(재실측 docx 는 그 부분을 다루지 않는다).
>
> 재실측이 추가로 밝힌 것: 확장 3사(머스크·HMM·하파그)가 전부 **원양 편중**이라 3사를 다 열어도
> 원양 커버리지는 45.60%가 되지만 **단거리(아시아 역내)는 17.59%에 머문다.** 단거리는 API 로 열리지
> 않으므로 국내 터미널 선석 연계로 보완한다.

## 0. 요약

**비용이 드는 선사는 쓰지 않는다**(2026-08-14 지시). 아래는 요금 실사를 거쳐 **영구 무료로
확인된 선사만** 남긴 것이다.

| 구분 | 선사 | 건수 | 비중 | 누적 |
|---|---|---|---|---|
| **가동 중** | **6사** | 19,611 | 16.39% | **16.39%** |
| **확장 예정(무료)** | 3사 | 21,628 | 18.08% | **34.47%** |
| 제외(유료·계약·불가) | 4사 | 14,708 | 12.30% | — |
| 롱테일·포워더 | 33종 외 | 63,684 | 53.23% | — |

무료만으로 **자사 컨테이너 물동량의 34.47%** 까지 자동 추적이 가능하다(현 시점 실가동 16.39%).

**2026-08-19 현행화로 바뀐 두 줄**

- **ZIM 개통(2026-08-18)** — 확장 예정 → **가동 중**. 이로써 live 6사(1.44%p 이동).
- **MSC 제외(2026-08-18 사용자 지시)** — 무료로 확인됐으나 **Data Sharing Agreement 서명이 필요**하다.
  계약·유료 선사는 전부 논외라는 방침에 따라 확장 예정 → 제외(5.71%p 이동). 요금 실사 결과(§2·§3)는
  방침이 바뀔 때를 대비해 기록으로 남긴다.

## 1. 가동 중 — 6사 (16.39%)

앞의 5사는 인증 없는 공개 JSON/HTML 을 쓰므로 **비용도 키도 없다.** ZIM 만 무료 키가 필요하다.
전부 태웅 실 B/L 로 검증 완료.

| 선사 | SCAC | LINE_CD | 건수 | 비중 | 데이터 출처 | 인증 |
|---|---|---|---|---|---|---|
| ONE | ONEY | LONE | 6,221 | 5.20% | ecomm.one-line.com | 없음 |
| Evergreen | EGLV | LEVER | 4,002 | 3.35% | ct.shipmentlink.com | 없음 |
| COSCO | COSU | LCOSCO+LCO | 3,981 | 3.33% | elines.coscoshipping.com | 없음 |
| SITC | SITC | LHY | 2,091 | 1.75% | ebusiness.sitcline.com | 없음 |
| SM상선 | SMLM | LSML01 | 1,593 | 1.33% | esvc.smlines.com | 없음 |
| **ZIM** | **ZIMU** | LZIM01+LZIM | 1,723 | 1.44% | apigw.zim.com (DCSA T&T v2) | **구독키 + OAuth 2단** |
| | | | **19,611** | **16.39%** | | |

구현: `supabase/functions/carrier-track` 의 `CARRIERS` 레지스트리.

> **ZIM 개통 메모(2026-08-18 실측)** — 관문이 둘이다. `Ocp-Apim-Subscription-Key`(구독 키)와
> `Authorization: bearer <JWT>` 를 **둘 다** 보내야 통과하고, 토큰은 `apigw.zim.com/authorize/v1` 에서
> `grant_type=client_credentials` + **`scope=tracing`** 으로 받는다(게이트웨이가 내부 스코프로 번역하므로
> `login.microsoftonline.com` 직접 호출은 실패). **사용자당 동시호출 10건 제한이라 토큰 캐시는 선택이 아니라 필수**다.
> 시크릿 3종(`ZIM_API_KEY`·`ZIM_CLIENT_ID`·`ZIM_CLIENT_SECRET`)이 모두 있어야 `ready()` 가 참이 된다.

> 공통 주의 — **모든 선사가 SCAC 프리픽스를 뗀 번호로 조회해야 한다.** 붙인 채 보내면
> HTTP 200 + 빈 응답이라 장애를 눈치채기 어렵다(COSCO 에서 실제로 겪음).

## 2. 확장 예정 — 무료 3사 (+18.08%p → 34.47%)

전부 **영구 무료**로 확인된 선사이며, **어댑터는 셋 다 개발·배포 완료**라 키 등록만 남았다
(키가 들어오면 `ready()` 게이트가 열려 **재배포 없이 다음 호출부터 실조회로 승격**된다).

| 순위 | 선사 | SCAC | 건수 | 비중 | 어댑터 | 발급 난이도 |
|---|---|---|---|---|---|---|
| 1 | 머스크 | MAEU | 9,618 | 8.04% | ✅ 완료 | 셀프+수동승인 |
| 2 | HMM | HDMU | 7,550 | 6.31% | ✅ 완료(2026-08-18) | 셀프+승인심사 |
| 3 | 하파그로이드 | HLCU | 4,460 | 3.73% | ✅ 완료 | **완전 셀프서비스** |
| | | | **21,628** | **18.08%** | | |

- **ZIM**(1,723건 1.44%)은 2026-08-18 **개통 완료** → §1 로 이동.
- **MSC**(6,826건 5.71%)는 무료지만 **계약 서명이 필요해 제외** → §3. 요금 근거는 아래에 남긴다.

### 무료 근거 (2026-08-14 실사 — 공식 스펙·약관·카탈로그 직접 확인)

| 선사 | 근거 | 무료 한도 |
|---|---|---|
| 머스크 | 카탈로그 `monetized:false` · `trialDuration:0` | 4,000콜/시간 (스파이크 60콜/분) |
| 하파그로이드 | 카탈로그 `price:"Free"` · 약관 §5.1 "free of charge" | Basic 6,000콜/일 — **초과 시 과금 아닌 429 차단** |
| HMM | 요금·플랜·결제 페이지가 포털에 존재하지 않음 | 공개 수치 없음 |
| ZIM | EULA 1.7 "Currently ... at no cost" | 사용자당 동시 호출 10건 |
| MSC | 공식 FAQ — 직접 연결은 무과금 | 100,000콜/일 · 초당 4콜 |

### 선사별 제약

- **머스크**: 태웅이 머스크에 **직접 부킹한 화물만** 조회된다(고객코드 당사자 아니면 404).
  자사 물동량 추적이 목적이니 문제없다. 신청 시 **Track and Trace Plus** 를 골라야 하며
  **Ocean Track & Trace Public Access 는 유료**다.
- **하파그로이드**: BETA 단계라 초기에는 웹 대사 검증 병행 권장.
- **MSC**: 반드시 **직접(direct) 연결**. 제3자 벤더를 경유하면 유료가 된다.
- **ZIM**: 30일 사전 서면통지만으로 유료 전환 가능(약관 명시).

## 3. 제외 — 유료·계약이거나 API 없음 (12.30% 포기)

| 선사 | 건수 | 비중 | 제외 사유 |
|---|---|---|---|
| MSC | 6,826 | 5.71% | **무료지만 Data Sharing Agreement 서명 필요** — 계약·유료 선사 전면 제외 방침(2026-08-18 사용자 지시) |
| CMA CGM | 6,167 | 5.16% | **영구 무료 티어 없음** — 30일 체험 후 접근 비활성. 유료는 월정액+초과단가이며 **400 응답까지 과금 대상** |
| 완하이 | 909 | 0.76% | 공개 API 자체가 없음 |
| OOCL | 806 | 0.67% | 계열사 IQAX 유료 구독, 자가가입 불가 |
| | **14,708** | **12.30%** | |

## 4. 자동화 불가 — 참고

무료·유료를 떠나 **기술적으로 서버 자동 조회가 안 되는** 선사들이다.

| 구분 | 선사 | 건수 | 비중 | 사유 |
|---|---|---|---|---|
| 봇 차단 | 흥아·장금·고려해운·양밍 | 27,840 | 23.27% | TLS 지문·IP 계층 차단(실측). 사무실 PC Playwright 필요 |
| 추적 화면 없음 | 팬오션·남성해운 | 10,344 | 8.65% | 공개 추적 페이지가 존재하지 않음(실측). 선사 협의 외 방법 없음 |

봇 차단 4사는 **사무실 PC 에 Playwright + 실 Chrome 상주** 방식으로 별도 수집기를 만들면
가능하다(결과만 서버로 업로드). 우회 기법(지문 위조·프록시 로테이션)은 만들지 않는다 —
약관 위반이고 상대가 갱신하면 바로 깨져 유지가 불가능하다.

## 5. 실행 순서

~~0. **ZIM 신청·개통**~~ **완료(2026-08-18)** — 6번째 live 선사. 개통 방식이 그대로 남은 3사의 선례다.

1. **하파그로이드** — 돈·계약·영업 접촉 없이 혼자 끝나는 유일한 선사. 현재 **계정 승인 대기**(계정 승인 → 앱 생성 → 상품 구독 3단계)
2. **HMM** — 국내 선사, 한국어 지원, 결제 입력란 없음. 현재 **승인 심사 대기**(8월 중순 신청)
3. **머스크** — 상품 선택만 주의(**Track and Trace Plus**, Public Access 는 유료). 관문은 **고객코드 확보**
4. ~~ZIM·MSC 신청~~ — ZIM 완료, **MSC 는 계약 서명 요구로 제외**(§3)
5. ~~HMM·ZIM·MSC 어댑터 개발~~ **어댑터는 전부 완료·배포됨** — 남은 것은 키뿐이다

**코드 작업은 남아 있지 않다.** 머스크·하파그·HMM 셋 다 DCSA 공용 파서를 쓰는 어댑터가 배포돼 있고,
**키 등록 즉시 재배포 없이 실조회로 승격**된다(`ready()` 게이트 — ZIM 개통으로 실증된 방식).
1~3번을 모두 완료하면 커버리지가 **16.39% → 34.47%** 가 된다.

포털 문의는 글로벌 큐에서 지연되므로, 각 선사 **영업 담당자를 통한 내부 요청이 실효적**이다
(2026-08-19 실행 계획 — 상세는 정본 docx §8).

> **선사를 추가할 때 갱신 지점은 두 곳이다** — `carrier-track` 의 `CARRIERS` 레지스트리와
> `bl-watch` 의 `LIVE` 목록. 후자를 빠뜨리면 **조회는 되는데 감시 등록만 "지원하지 않는 선사"로
> 거부된다**(ZIM 개통 당시 실제로 발생한 사고).

## 6. 공통 리스크

무료로 확인된 5사 **전부** 약관에 유료화 권리를 유보하고 있다. 영구 무료를 계약으로
보장한 선사는 없다(머스크 License Terms 4조 · HMM 약관 제17조① · ZIM EULA 1.7 ·
하파그 §5.1).

**대응**: 선사에서 요금 관련 통지가 오면 **Supabase Secrets 에서 해당 키만 삭제**한다.
`ready()` 가 false 가 되어 즉시 딥링크로 되돌아가며, 비용 발생 전에 차단된다.

이 롤백 경로는 이제 **가정이 아니라 실가동 중인 안전장치**다 — ZIM 이 키 기반으로 개통됐으므로
(`ZIM_API_KEY`·`ZIM_CLIENT_ID`·`ZIM_CLIENT_SECRET`), ZIM 이 EULA 1.7 의 30일 통지로 유료 전환을
알려오면 키 3종을 지우는 것만으로 즉시 딥링크로 복귀한다. 코드 수정도 재배포도 필요 없다.

## 7. 물동량 재집계 방법

```sql
SELECT LINE_CD, COUNT(*)
  FROM ELVIS_TWSC.FMS_MBL_MST
 WHERE ONBD_YMD >= '20250101'
   AND CNTR_TYPE IN ('F','L')   -- FCL·LCL 만. 'B'(벌크) 제외
   AND LINE_CD IS NOT NULL
 GROUP BY LINE_CD ORDER BY 2 DESC
```

- `SHIP_TYPE` 은 D/C 두 값 모두 같은 선사가 나타나므로 **해상/항공 구분이 아니다.** 필터에 쓰면 안 된다.
- 같은 선사가 대리점·서비스별로 복수 `LINE_CD` 를 갖는다. **합산하지 않으면 순위가 왜곡된다**:
  하파그 = `LHAPAG1`+`LHAPAG`+`LHAPAGT` · COSCO = `LCOSCO`+`LCO` · CMA = `LCMA`+`LCNCL` · ZIM = `LZIM01`+`LZIM`
- `LINE_NM` 에 오염 데이터가 섞여 있다(`LHDMU` 에 "주)", `LNMS` 에 "장금상선" 1건 등).
  **`LINE_CD` 를 기준으로 잡고 이름은 최빈값으로 본다.**
