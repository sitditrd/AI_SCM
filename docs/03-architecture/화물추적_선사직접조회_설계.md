# 화물추적 — 선사 직접조회 설계 (KLNET 대체)

작성 2026-08-11 · 근거: TWSC/TW_BATCH 레거시 4계층 병렬 분석 + 선사 13사 엔드포인트 실호출 검증

## 1. 왜 만들었나

레거시 TWSC는 **KLNET PLISM 유료 API**(api.plism.com)로 MBL 추적 이벤트를 수집해
`KlnetTrackList` 화면(6밴드 53컬럼)에 표시한다. AI_SCM 포털의 cargo 탭은 이를 무료로 대체한다 —
사용자가 MBL을 입력하면 **선사 공개 트래킹 페이지의 백엔드 JSON API를 직접 호출**해
동급의 운송 이벤트 타임라인을 보여준다.

## 2. 레거시 파이프라인 (분석 결과 요약)

```
[부킹]  SeaBkgMgt: MBL_NO 수기입력 → OMS_BKG_MST → (House 생성 시) FMS_HBL_MST 복사
[등록]  SeaBkgList btnKLNETApi → TRK0002(bl_no, ref_no, carrier_code) → FMS_API_MST MERGE (시드)
[수집]  KlnetTrkAPIBatch(07·12시대): 기등록 MBL 중 ETD±3개월 && 공컨미반납 → TRK0003→TRK0005(20건 청크)
        → FMS_API_MST(BL)/FMS_API_TS(환적)/FMS_API_CNTR(컨테이너) delete-then-insert
[표시]  KlnetTrackList: FMS_API_* + 자사(FMS_HBL/MBL·OMS_BKG) LEFT JOIN → KLNET vs TAS_ 비교 표시
```

- **3레벨 데이터 모델**: BL(FMS_API_MST: POL/POD·VESSEL/VOYAGE·ETD/ATD·ETA/ATA·DIRECT_TS) ·
  환적구간(FMS_API_TS: SEQ별 항차, 화면은 2구간 절단) · 컨테이너 이벤트(FMS_API_CNTR: 게이트 이벤트 시각)
- **타임라인 캐논 순서**: 공컨반출(E_GATE_OUT) → 적컨반입(F_GATE_IN) → 출항(ETD/ATD) →
  환적₁…ₙ → 도착(ETA/ATA) → 적컨반출(F_GATE_OUT) → 공컨반납(E_GATE_IN) — 공컨반납이 추적 종료 판정
- **선사코드 4중 체계**: 자체 LINE_CD(LCMA·LONE…) / 표준 4자 `MDM_CARR_MST.KR_CUS_4CD`(CMDU·ONEY…) /
  KLNET carrier_code(CMAL·SSBF — SCAC과 다름) / BL 프리픽스. **KLNET조차 CMA그룹·완하이·SWIRE 6개
  선사군은 carrier_code를 별도로 받아야 했다** = 이들 MBL은 프리픽스 자기식별이 안 된다.
- 조회 키는 **MBL 단일**(bl_no). 시각은 YYYYMMDDHH24MISS 문자열, `0`/NULL = 미발생.

## 3. 선사별 서버사이드 조회 가능성 (2026-08-11 실측)

| 선사 | 방식 | 판정 | 근거 |
|---|---|---|---|
| **ONE (ONEY)** | `ecomm.one-line.com/api/v2/edh` JSON | ✅ **live 어댑터** | 실 BL(RICG34548800)로 curl·Supabase Edge 양쪽 검증 — search/cop-events/voyage-list 3종 |
| **COSCO (COSU)** | `elines.coscoshipping.com/ebtracking/public/bill/{no}` | ✅ live(베스트에포트) | 무인증 JSON 확인. 실 BL 표본 없어 필드 매핑은 관대 추출 |
| Evergreen (EGLV) | shipmentlink 폼 POST → HTML | ⚠️ 가능성 (미구현) | 200 응답, HTML 파싱 필요 |
| SM상선 (SMLM) | esvc.smlines.com JSP | ⚠️ 가능성 (미구현) | 200 응답 |
| 장금상선 | ebiz.sinokor.co.kr | ⚠️ 파라미터 미확정 | 500 (엔드포인트 존재) |
| HMM (HDMU) | www.hmm21.com | ❌ 딥링크 | **비브라우저 TLS를 TCP 계층에서 드랍** (실 Chrome은 정상) |
| KMTC | api.ekmtc.com (`POST /trans/trans/cargo-tracking/`) | ❌ 딥링크 | Akamai TLS 지문 차단 (경로·바디는 확보: `{dtKnd:'BL',blNo}`) |
| MSC·Maersk·Hapag·CMA·OOCL·YML·완하이 | — | ❌ 딥링크 | 403 안티봇 (Akamai/Cloudflare) |

**핵심 교훈**: 안티봇은 URL이 아니라 **TLS 지문·IP 평판**을 본다. 이 PC(법인망 curl)에서 403이면
Edge Function(데이터센터 IP)에서도 403이다. 반대로 ONE·COSCO는 양쪽 다 열려 있음을 실측으로 확인했다.

### ONE API 상세 (검증 완료)

| 용도 | 호출 | 비고 |
|---|---|---|
| 검색(컨테이너 목록) | `POST /api/v2/edh/containers/track-and-trace/search` body `{page,page_length,filters:{search_text,search_type:"ALL"},timestamp}` | search_text = **ONEY를 뗀 12자 부킹번호** (MBL 원형은 0건 — 실측) |
| 이벤트 타임라인 | `GET /api/v2/edh/containers/track-and-trace/cop-events?booking_no&container_no` | `eventName`·`eventLocalPortDate`(현지)·`eventDate`(UTC)·`triggerType`(ACTUAL/예정)·`copSequence`(순서)·`location`·`yard`·`matrixId`(이벤트코드 E012 등)·`cargoTrackingShow`(노출 플래그) |
| 항차(N구간) | `GET /api/v2/edh/vessel/track-and-trace/voyage-list?booking_no` | `vesselEngName`·`scheduleVoyageNumber+DirectionCode`·pol/pod의 `isActual` |

## 4. 구현 (2026-08-11)

- **Edge Function `carrier-track`** (verify_jwt=false·시크릿 없음) — datago의 화이트리스트 사상 계승:
  - `?no=<BL>&carrier=<SCAC?>` → 프리픽스 자동 감지, `CARRIERS` 맵(live: ONEY·COSU)만 실조회
  - 미지원 선사 → `supported:false` + 딥링크 반환 (화면이 기존 딥링크 폴백)
  - 정규화 계약(레거시 3레벨 계승): `summary`(FMS_API_MST 상당) / `voyages[]`(FMS_API_TS 상당, **N구간 — 레거시의 2구간 절단을 계승하지 않음**) / `containers[].events[]`(FMS_API_CNTR 상당)
  - 이벤트 시각: `timeLocal`(항만 현지 — 기본 표시·레거시 관례) + `timeUtc`(보조), `actual` 불리언
  - `?api=list` = live/딥링크 선사 목록 소개
- **cargo 화면**: 선사 직접 추적 전용으로 재편 — 해상 MBL 감지(oceanInfo) 시 live 선사면 `#carrierOut`에 선사 운송 카드
  (구간 요약 → 항차 표 → 컨테이너별 타임라인·실제/예정 배지·현지시각), 미지원 선사·AWB는 공식 추적 딥링크 카드.
  **유니패스 통관조회는 화면에서 제외**(2026-08-11 사용자 결정 — Edge Function `track`은 보존만, 화면 미사용).
  SCAC 감지 정규식은 영숫자 완화(`[A-Za-z]{4}[A-Za-z0-9]{6,}`) — ONE 부킹오피스 문자 BL 대응(실측 버그 수정).
- **적용한 레거시 함정 대응**: 컨테이너 번호 영숫자 정규화 · 시각 미발생 생략 · 현지시각을 `new Date()` 변환
  없이 문자열로 표시(TZ 왜곡 방지) · E/A 구분 배지 · 신선도(fetchedAt·source) 표기 · 3단 방어 파싱 사상

## 5. 남긴 것 (로드맵)

1. **COSU 필드 매핑 확정** — 실 COSCO BL 확보 시 응답 구조 대조·정밀 매핑
2. **Evergreen·SM상선·장금 어댑터** — HTML 폼 파싱 (실 BL 필요)
3. **선사 선택 폴백 UI** — live 어댑터가 늘어 자기식별 안 되는 MBL(CMA그룹 등)이 지원 대상이 되면 추가
4. **멀티 조회**(레거시 M 모드: 줄바꿈 입력→IN) — 실무자 마이그레이션 시 최우선 요망 기능
5. **HBL→MBL 해석** — TAS 연계 또는 유니패스 hblNo 조회로 MBL 역추적
6. **캐시(TTL)·레이트리밋** — 호출량 증가 시. "빈 응답은 기존 데이터 보존" 원칙(레거시 결함 §3-7 교훈)
7. **TAS 자사값 비교(TAS_ETD vs 선사 ETD)** — Oracle 연계 별도 과제

## 6. 참고 — 레거시 소스 위치

- 화면: `D:\TWSC\YJIT.SC.FMS\SEA\KLNETTrackList\` · Biz: `D:\TWSC\WebApp\App_Code\YJIT.Biz\SEA\KlnetTrackList.vb` · SQL: `App_Data\PLSQL\SEA\KlnetTrackList.sql` (SqlBox 인라인, 프로시저 아님)
- 배치: `C:\TW_DEV\workspace\TW_BATCH\src\core\com\twsc\batchworks\klnet\KlnetTrkAPIBatch.java`
- 부킹: `SeaBkgList.vb`·`SeaBkgMgt.vb` · KLNET 등록: `PLSQL\EDI\KLNETApiInterface.sql`
- 오라클: `ELVIS_TWSC.FMS_API_MST/TS/CNTR`(KLNET 수신) · `TRSA.FMS_TRK_*`(TAS 신규, DEV 0건)
